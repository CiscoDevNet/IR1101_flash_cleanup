Access the router via SSH, Telnet, or the console and enter configuration mode, 
and input the EEM script provided to remove the .pkg and .conf files from the flash. 
The EEM script will automatically exit once the files are deleted.
